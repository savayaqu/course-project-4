<?xml version="1.0" encoding="UTF-8" ?>
<?xaml-comp compile="true" ?>
<ResourceDictionary 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <!-- Стиль для фона -->
    <Style x:Key="PageBackgroundStyle" TargetType="ContentPage">
        <Setter Property="BackgroundColor" Value="{DynamicResource BackgroundColorLight}" />
    </Style>

    <!-- Стиль для текста -->
    <Style x:Key="TextStyle" TargetType="Label">
        <Setter Property="TextColor" Value="{DynamicResource TextColorLight}" />
    </Style>

    <!-- Стиль для кнопок -->
    <Style x:Key="ButtonStyle" TargetType="Button">
        <Setter Property="BackgroundColor" Value="{DynamicResource ButtonBackgroundColorLight}" />
    </Style>

</ResourceDictionary>
